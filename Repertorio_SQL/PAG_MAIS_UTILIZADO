select MEIO_PAGTO
      ,SUM(QUANTIDADE) AS QUANTIDADE
      ,COUNT (*) AS TIPO_PAGAMENTO
      ,SUM(VALOR_UNITARIO) AS VALOR

 
 from pm_web.pm_web_resultado
 GROUP BY MEIO_PAGTO
 ORDER BY QUANTIDADE DESC
